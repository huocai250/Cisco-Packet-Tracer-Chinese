# 更新日志

## v1.0.0 (2026-09-16)

首个发布版本。

- 基于官方 `languages\default.ts` 模板完成 9154 / 10603 条界面文本翻译（约 86.3%，去重后 6613 条独立字符串）。
- 覆盖菜单、首选项、设备配置窗口、活动向导、模拟模式与 PDU 解释、无线/工业 IoT 界面、证书与错误对话框。
- 保留 `&` 加速键、`%1` / `%n` / `[[VAR]]` 占位符与 HTML 富文本结构，并加入自动校验脚本。
- 按上下文区分同形词（`Fire` → PDU 列表「发射」/ 环境面板「火焰」）。
- 新增可选的设备箱（Device Box）中文补丁：替换 exe 内置 `DeviceTypesConfig.json` 的 53 条界面文字，压缩后字节数与原始完全一致（1919 字节），不改变 exe 长度。
- 设备箱补丁已在 Windows 版 Packet Tracer 9.0.1 上实测生效（`--apply` / `--restore` 均验证通过）。
- 提供可复现的翻译流水线（`tools/`）与纯文本译文分片，便于社区继续完善与提交 PR。
